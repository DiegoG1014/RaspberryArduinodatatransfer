class bytearray {
  private:
    void array;
  public:
    bool add(void, int);
    bool rem(int);
    bool destruct();
    bool get(int,int);
    bool isValid();
    bool reset();
    bool set(void);
    int  bitindex;
    int  len;
};

